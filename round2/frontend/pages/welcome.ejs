<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Welcome</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="../css/main.css">
  <link rel="stylesheet" href="../css/components.css">
</head>

<body class="h-screen w-screen bg-gradient-to-r from-indigo-500 via-rgba(44, 62, 80, 0.95) to-blue-500 ">

   <!-- ENHANCED RESPONSIVE NAVIGATION BAR -->
<nav class="mb-8">
  <div class="nav-container">
    <!-- Logo -->
    <div class="logo">
      <img src="/images/WhatsApp Image 2025-08-09 at 16.46.01_0b7ada1a.jpg" alt="FinLit Logo">
      <div>FinLit</div>
    </div>

    <!-- Mobile Menu Toggle Button (will be created by JavaScript) -->
    <!-- Navigation Links -->
    <div class="nav-links">
      <a href="/">Home</a>
        <a href="/budget">Budget</a>
        <a href="/savings">Savings</a>
        <a href="/demat-simulator">Demat Simulator</a>
        <a href="/quiz">Quiz</a>
        <a href="/investments">Investments</a>
        <a href="/welcome" class="active">Login</a>
    </div>
  </div>
</nav>

<!-- Alerts -->
  <% if((typeof(error) !=="undefined" ) && error.length> 0) { %>
    <div id="error-box" class="z-[999] absolute top-5 left-1/2 -translate-x-1/2 p-3 rounded-md bg-red-500 shadow-lg">
      <span class="inline-block mt-1 mb-1 text-white">
        <%= error %>
      </span>
      <div id="progress-bar" class="h-1 bg-white mt-2"></div>
    </div>
  <% } %>
  <% if((typeof(success) !=="undefined" ) && success.length> 0) { %>
    <div id="error-box" class="z-[999] absolute top-5 left-1/2 -translate-x-1/2 p-3 rounded-md bg-blue-500 shadow-lg">
      <span class="inline-block mt-1 mb-1 text-white">
        <%= success %>
      </span>
      <div id="progress-bar" class="h-1 bg-white mt-2"></div>
    </div>
  <% } %>

  <div class="flex flex-col gap-5 items-center justify-center font-sans text-white mt-[25]">

  <!-- Chooser -->
  <div id="chooser" class="text-center space-y-6">
    <h2 class="text-4xl font-bold mb-2">Welcome to <span class="text-green-400">FinLit Portal</span></h2>
    <p class="text-lg">Choose your path</p>
    <div class="flex justify-center gap-6 mt-6">
      <button onclick="showForm('register')" class="btn btn-primary bg-green-500 text-white px-6 py-3 rounded-lg hover:bg-green-600 shadow-md transition z-[100]">Register</button>
      <button onclick="showForm('login')" class="btn btn-primary bg-white text-gray-800 px-6 py-3 rounded-lg hover:bg-gray-200 shadow-md transition z-[100]">Login</button>
    </div>
  </div>

  <!-- Form -->
  <div id="form" class="bg-white text-gray-800 shadow-2xl rounded-xl p-10 w-full max-w-4xl flex flex-col md:flex-row gap-8 hidden">
    <!-- Register Section -->
    <div id="registerForm" class="flex-1 hidden">
      <h2 class="text-2xl font-semibold mb-2 text-indigo-600">Register</h2>
      <p class="text-sm mb-6 text-gray-600">Flow freely. Create your rhythm.</p>
      <form class="flex flex-col gap-4" method="post" action="/users/register">
        <input type="text" placeholder="Full Name" class="p-3 rounded-md border border-gray-300 focus:outline-none focus:ring-2 focus:ring-green-500" name="fullname">
        <input type="email" placeholder="Email" class="p-3 rounded-md border border-gray-300 focus:outline-none focus:ring-2 focus:ring-green-500" name="email">
        <input type="password" placeholder="Password" class="p-3 rounded-md border border-gray-300 focus:outline-none focus:ring-2 focus:ring-green-500" name="password">
        <button class="bg-green-800 text-white py-2 rounded-md hover:bg-green-600 transition">Create Account</button>
      </form>
    </div>

    <!-- Login Section -->
    <div id="loginForm" class="flex-1 hidden">
      <h2 class="text-2xl font-semibold mb-2 text-indigo-600">Login</h2>
      <p class="text-sm mb-6 text-gray-600">Already flowing? Dive back in.</p>
      <form class="flex flex-col gap-4" method="post" action="/users/login">
        <input type="email" placeholder="Email" class="p-3 rounded-md border border-gray-300 focus:outline-none focus:ring-2 focus:ring-green-500" name="email">
        <input type="password" placeholder="Password" class="p-3 rounded-md border border-gray-300 focus:outline-none focus:ring-2 focus:ring-green-500" name="password">
        <button class="bg-green-800 text-white py-2 rounded-md hover:bg-green-600 transition">Login</button>
      </form>
    </div>
  </div>
  </div>

  <!-- Script -->
  <script>
    function showForm(type) {
      document.getElementById('form').classList.add('hidden');
      document.getElementById('registerForm').classList.add('hidden');
      document.getElementById('loginForm').classList.add('hidden');

      if (type === 'register') {
        document.getElementById('form').classList.remove('hidden');
        document.getElementById('registerForm').classList.remove('hidden');
      } else if (type === 'login') {
        document.getElementById('form').classList.remove('hidden');
        document.getElementById('loginForm').classList.remove('hidden');
      }
    }
  </script>

<script src="../js/welcome.js"></script>
<script src="../js/main.js"></script>

</body>
</html>

